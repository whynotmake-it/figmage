name: Continuous Integration

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  semantic_pull_request:
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/semantic_pull_request.yml@v1

  flutter-check:
    name: Build Check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      id-token: write
      contents: read
      checks: write
      pull-requests: write
    steps:
      - name: 📚 Checkout
        uses: actions/checkout@v4
      
      - name: 🎯 Setup Dart
        uses: dart-lang/setup-dart@v1.5.1
        with:
          sdk: 'stable'
      
      - name: 📊 Generate Coverage
        id: coverage-report
        uses: whynotmake-it/dart-coverage-assistant@0dd7963db792b61c24bf84ced7ffac808e386292
        with:
          enforce_threshold: 'none'
          lower_threshold: 1
          upper_threshold: 3
      
      - name: 💬 Add sticky comment
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          message: ${{ steps.coverage-report.outputs.message }}
          
  