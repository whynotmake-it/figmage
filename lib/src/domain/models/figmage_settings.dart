import 'package:figmage/src/domain/models/config/config.dart';

/// All settings that are used for the `forge` command.
typedef FigmageSettings = ({
  String token,
  String fileId,
  String path,
  Config config,
});
