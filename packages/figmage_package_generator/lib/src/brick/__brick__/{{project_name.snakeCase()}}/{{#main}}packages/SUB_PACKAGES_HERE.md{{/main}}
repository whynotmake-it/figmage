## Sub-Packages

Sub-Packages for {{project_name.snakeCase()}} go in this folder.

You can just run `mason make wnma_dart_package` to create a new sub-package.

**Make sure to select `n` when asked if this is a main package.**

